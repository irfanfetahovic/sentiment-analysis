# Optimized dependencies for Docker deployment (CPU-only, inference-focused)
# Expected image size: ~3-5GB (vs ~13GB with full requirements.txt)

# =============================================================================
# CPU-ONLY PyTorch - saves ~2GB compared to CUDA version
# =============================================================================
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# =============================================================================
# Core ML Libraries
# =============================================================================
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
transformers>=4.30.0

# Removed for inference (not needed, saves ~600MB):
# - datasets>=2.14.0  (only needed for training)
# - gensim>=4.3.0     (only needed for Word2Vec training)
# - evaluate>=0.4.0   (only needed for evaluation)
# - matplotlib, seaborn (only needed for visualization)

# =============================================================================
# NLP Libraries
# =============================================================================
nltk>=3.8.0
contractions>=0.1.73
emoji>=2.8.0
beautifulsoup4>=4.12.0

# Text Processing
regex>=2023.8.8
langdetect>=1.0.9

# =============================================================================
# Web Framework (FastAPI only - Flask removed to save space)
# =============================================================================
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
slowapi>=0.1.9
gunicorn>=21.2.0

# =============================================================================
# Utilities
# =============================================================================
python-dotenv>=1.0.0
PyYAML>=6.0
joblib>=1.3.0
tqdm>=4.65.0
